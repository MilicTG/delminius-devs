import React, { Component } from "react";
import styled from "styled-components";
import { colorHeader } from "../styles";

export const Header = () => {
  return <HeaderWrapper>del</HeaderWrapper>;
};

//styles

const HeaderWrapper = styled.header`
  width: 100%;
  height: 90vh;
  background: ${colorHeader};
`;
